import ContactForm from "@/features/contact-form/ui/Form";

const Contact = () => {
  return (
    <section style={{ padding: "var(--space-8) 1rem" }}>
      <ContactForm />
    </section>
  );
};

export default Contact;
